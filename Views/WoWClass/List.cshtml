@model IEnumerable<PassionProjectTwo.Models.WoWClass>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClassName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClassSpec)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClassPic)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PicExtension)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClassName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClassSpec)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClassPic)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PicExtension)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClassID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClassID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClassID })
        </td>
    </tr>
}

</table>
